# directories

.coverage_artifacts .coverage_cache .coverage_contracts artifacts build cache coverage dist docs node_modules types

# files

_.env _.log .DS_Store .pnp.\* coverage.json package-lock.json pnpm-lock.yaml yarn.lock
